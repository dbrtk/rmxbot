#
# from . import celeryconf
# from rmxbot.run import app, make_celery
#
# celery = make_celery(app)
#
# celery.config_from_object(celeryconf)
# celery.autodiscover_tasks([__name__, 'tassk.corpus', 'tasks.data'])
